fn main() {
    let iters = 10000;
    let total_iters = iters * iters;

    let sum = 0 + 0 + 0 + 0 + 0 + 0 + 0 + 0;
    println(sum);
    for i in 0..iters {
        for j in 0..iters {
            sum += 1;
        }
    }
    println(sum);
    assert sum == total_iters;
}